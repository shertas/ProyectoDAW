# Copia este fichero a .env y rellena los valores locales. NO subir .env al repositorio.

# Base de datos Aiven (ejemplo)
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASS=
DB_SSL=true

# Opcional: otras variables de entorno que use el proyecto
# APP_ENV=development
# APP_DEBUG=true
